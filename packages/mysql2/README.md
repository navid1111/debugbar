# @debugbar/mysql2

Transparent Promise and callback instrumentation for `mysql2` connections and pools. Query result rows are never captured.

See the [database guide](../../docs/guide.md#database-adapters).
