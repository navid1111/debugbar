# @debugbar/better-sqlite3

Request-local `better-sqlite3` query instrumentation for Express + React Debugbar. Query result rows are never captured.

See the [database guide](../../docs/guide.md#database-adapters).
